<% if @diaries.present? %>
  <div class="row mt-1 justify-content-center">
    モチベーションチャート
  </div>
  <%= line_chart @score_data, library: { scales: { y: { min: -1, max: 1, stepSize: 0.1 } } } %>
<% end %>