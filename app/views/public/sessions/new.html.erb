<div class="row justify-content-center">
  <div class="col-6 bg-dark rounded-pill d-flex justify-content-center pt-2 mt-5">
    <%= image_tag 'motilog.png', size: "300x140" %>
  </div>
</div>

<div class="row justify-content-center">
  <div class="col-6">
    
    <div class="row">
      <div class= "col-5">
        <p class="font-weight-bold bg-secondary text-light text-center mt-4 h4">
        ログイン
        </p>
      </div>
    </div>
    
<%= form_with model: @user, url: root_path do |f| %>
    <div class="row">
      <div class= "col-5">
        <p class="font-weight-bold bg-secondary text-light text-center mt-4 h4">
        ログイン
        </p>
      </div>
    </div>
<% end %>
  </div>
</div>

<div class="row">
</div>







<h2>Log in</h2>

<%= form_with model: @user, url: root_path do |f| %>
  <div class="field">
    <%= f.label :メールアドレス %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <div class="field">
    <%= f.label :パスワード %><br />
    <%= f.password_field :password, autocomplete: "current-password" %>
  </div>

  <% if devise_mapping.rememberable? %>
    <div class="field">
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me %>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit "Log in" %>
  </div>
<% end %>

<%= render "public/shared/links" %>
